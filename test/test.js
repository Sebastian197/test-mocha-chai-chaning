describe('Sumas', () => {
    // Suma de + con + tiene que ser +
    it('Suma de + con + tiene que ser +', () => {
        console.log(2 + 2);
        console.log(4);
    });
    // Suma de + y mayor que el segundo elemento - = +
    it('Suma de + y mayor que el segundo elemento - = +', () => {
        console.log(3 + (-2));
        console.log(1);
    });
});
